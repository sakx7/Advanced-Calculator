function enter(keyinputs, paramater, latexDisplay) {
    const inputs = {
        keyinputs: keyinputs,
        paramater: paramater,
        latexDisplay: latexDisplay 
    };
    console.log(inputs)
    return inputs;
}

export { enter };
